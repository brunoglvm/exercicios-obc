const nomes = ['Lucas', 'Mariana', 'Gabriel', 'Beatriz', 'Rafael', 'Camila', 'André', 'Juliana', 'Felipe', 'Larissa'];

for (let i = nomes.length - 1; i >= 0; i--) {
  const nome = nomes[i];
  console.log(nome);
}
